### Session — 2026-06-29b

**What we did:**
- Started scaffolding an ACS/WFC parallel pipeline alongside the existing WFC3/IR pipeline
- Created `01_download_data_acs.ipynb` by adapting the WFC3 download notebook: switched
  instrument to ACS/WFC, FLT→FLC globs, FILTER→FILTER1/FILTER2 keyword handling,
  config keyed to new `acs_download` section
- Created `03_drizzle_acs.ipynb` similarly: points to `data_acs/`, CR rejection enabled
  (ACS CCDs need it), reads `acs_drizzle` config (final_scale=0.03, final_pixfrac=0.4),
  inspection cell globs `*_drc.fits` (ACS FLC output suffix from AstroDrizzle)
- Created `data_acs/` folder tree (raw/, processed/) to isolate ACS data from WFC3
- Downloaded ACS data; discovered 5 quasars (not 4 initially assumed):
    - W2M0035+0114:  8 FLC + 8 FLT
    - W2M0043+0052:  4 FLC + 12 FLT
    - W2M1106+0221:  4 FLC + 12 FLT
    - W2M1242+0440:  8 FLC + 8 FLT
    - W2M2152-0051:  0 FLC + 16 FLT ← all FLT, needs CTE correction before drizzling
- Fixed several bugs introduced during adaptation: YAML indent error on product_sub_group,
  `*_drz.fits` glob changed to `*_drc.fits`, cwd crash vulnerability fixed using git
  repo root anchoring instead of relative paths
- Added `acs_download` and `acs_drizzle` sections to config YAML
- Created `01b_check_acs_flt.ipynb` diagnostic notebook to confirm FLT availability
- WFC3 drizzle notebook fixed: imports cell moved to position 1, font sizes increased,
  inspection PNGs saved to drizzled/ and GlikmanFIles/<target>/

**Decisions made:**
- ACS pipeline will be rebuilt from scratch next session, cell-by-cell with the user
  providing each task. Copying from WFC3 introduced too many silent assumptions and
  required repeated patching — a clean build alongside the agent will be more reliable.
- `data_acs/` is the permanent home for ACS data, fully separate from `data/` (WFC3).
  File structure is currently messy (ACS target folders still exist in data/raw/) —
  cleanup is part of the next-session rework.
- W2M1042+1641 remains dropped from the WFC3 active pipeline (TweakReg code kept).
- FLT-heavy targets (W2M0043+0052, W2M1106+0221, W2M2152-0051) need CTE correction
  via CalACS before they can be drizzled — downloading raw files is a prerequisite.

**Open issues at end of session:**
- ACS target folders (W2M0035+0114, W2M0043+0052, W2M1106+0221, W2M1242+0440) still
  exist in data/raw/ alongside WFC3 data — file structure needs cleanup
- W2M2152-0051 has 16 FLT files and 0 FLC — all need CTE correction before drizzling
- W2M0043+0052 and W2M1106+0221: only F475W drizzled (F814W files are FLT, not yet corrected)
- ACS drizzled outputs (drc.fits) are unverified — inspection cell not yet run to completion
- WFC3 drizzled outputs remain unverified from prior session
- WFC3: updatehdr=False — Gaia corrections not written to any FLT headers
